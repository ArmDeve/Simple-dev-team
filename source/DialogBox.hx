import flixel.group.FlxSpriteGroup;

class DialogBox extends FlxSpriteGroup // in progress
{
    public function new(boxType:String = 'normal', isPixel:Bool = false)
        {
            super();

            if (isPixel)
                {
            switch (boxType)
            {
            case 'normal':
            
            case 'angry':
            }
                }
            else
                {
            switch (boxType)
            {
            case 'normal':
            
            case 'angry':
            }
                }
        }
}